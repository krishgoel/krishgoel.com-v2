# name: Socials Fetch Workflow

# on:
#   # push: # Disabled because this creates a perpetual loop, every action causes a push which triggers the actions again
#   #   branches:
#   #     - main
#   # schedule:
#   #   - cron: "0 1 * * *"
#   workflow_dispatch:

# jobs:
#   refresh-feed:
#     runs-on: ubuntu-latest
#     steps:

#       - name: Fetch Socials from Potion
#         uses: JamesIves/fetch-api-data-action@releases/v1
#         with:
#           ENDPOINT: https://potion-api.vercel.app/table?id=fe3fe0e8c9e54061b7412307da4c035f
#           SAVE_LOCATION: static/data
#           SAVE_NAME: socials

#       - name: Update socials.json
#         uses: test-room-7/action-update-file@v1
#         with:
#           file-path: static/data/socials.json
#           commit-msg: Updated socials.json 
#           github-token: ${{ secrets.TOKEN }}