# name: Leadership Fetch Workflow

# on:
#   # push: # Disabled because this creates a perpetual loop, every action causes a push which triggers the actions again
#   #   branches:
#   #     - main
#   # schedule:
#   #   - cron: "0 1 * * *"
#   workflow_dispatch:

# jobs:
#   refresh-feed:
#     runs-on: ubuntu-latest
#     steps:

#       - name: Fetch Leadership Events from Potion
#         uses: JamesIves/fetch-api-data-action@releases/v1
#         with:
#           ENDPOINT: https://potion-api.vercel.app/table?id=e7ef2b0265ef4205a767e0ae3f6acbe4
#           SAVE_LOCATION: static/data
#           SAVE_NAME: leadership

#       - name: Update leadership.json
#         uses: test-room-7/action-update-file@v1
#         with:
#           file-path: static/data/leadership.json
#           commit-msg: Updated leadership.json 
#           github-token: ${{ secrets.TOKEN }}
